# Example Template Code For Flask API

In Render.com. upload your api then keep everything the same except from the Start Command. Chagne the Start Command to gunicorn main:app . main is the file name.
